api.php
config.php
db_config.php
turn.php
*.log
*.txt
sql_exequte.php
create_backup.php
upload_import_db.php
*.env
*.sql
.txt
vendor/

# Ausnahme: die versionierten Migrationsskripte.
# Die Regel *.sql weiter oben schliesst SQL-Dumps aus, hat aber auch die
# Dateien unter migrations/ erfasst - sie wurden dadurch beim Committen
# stillschweigend uebersprungen. Sie gehoeren ins Repository.
!migrations/
!migrations/*.sql

# Ausnahme: der Grundstock der Sprachratsche.
# Dieselbe Falle wie bei den Migrationen darueber: Die Regel *.txt weiter oben
# meint abgelegte Textdateien, erfasst aber auch tests/i18n_grundstock.txt -
# und die ist die halbe Pruefung. Ohne sie im Repository laeuft der Test bei
# jedem anderen ins Leere und meldet jedes vorhandene deutsche Literal als neu.
!tests/i18n_grundstock.txt

# Die Kennungen der Demodaten fuer die Aufnahmen der Landingpage.
# Geschrieben von tools/landing_seed.php, gelesen von tools/landing_shots.js -
# ein Zwischenstand der Entwicklungsdatenbank und in keinem anderen Arbeitsplatz
# gueltig.
tools/landing_seed.json

# Die Offline-Fassung der Landingpage.
# Erzeugt von tools/landing_offline.js aus der laufenden Seite - also ein
# Ergebnis und keine Quelle. Im Repository waere sie eine zweite Fassung, die
# nach dem ersten Umbau still falsch ist; wer sie braucht, laesst das Werkzeug
# laufen (ein Aufruf, siehe README).
offline/
